
## Streamlit entrypoint
Run the app from the repository root using:

```bash
streamlit run dmp_qc_app.py
```

For the standalone PTEMP/SNWD dashboard, use:

```bash
streamlit run streamlit_app.py
```
